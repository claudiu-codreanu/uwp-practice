<Page
    x:Class="CoffeeShop.CompletePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoffeeShop"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Height="1000">
        <Image Source="Assets/color-logo.png" Style="{StaticResource LogoStyle}" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="300"/>
            </Grid.RowDefinitions>
            <Image Source="Assets/ticket.jpg"
                   HorizontalAlignment="Center"/>
            <RelativePanel VerticalAlignment="Center">
                <TextBlock Name="OrderTextBlock"
                           Text="Order"
                           FontSize="36"
                           RelativePanel.AlignHorizontalCenterWithPanel="True"/>
                
                <TextBlock Text="96"
                           FontSize="48"
                           RelativePanel.Below="OrderTextBlock"
                           RelativePanel.AlignHorizontalCenterWithPanel="True"/>
            </RelativePanel>
        </Grid>
    </StackPanel>
</Page>
